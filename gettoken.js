(webpackChunkdiscord_app.push([[''],{},e=>{m=[];
for(let c in e.c)m.push(e.c[c])}]),m).find(m=>m?.exports?.default?.getToken!==void 0).exports.default.getToken()



// This code is used to fetch the Discord token from the browser's local storage.
// It is intended to be run in a browser environment where the Discord web client is active.
// The token is then printed to the console.
// Note: Using selfbots is against Discord's Terms of Service and can result in account termination.
// So use this on temporary accounts only.